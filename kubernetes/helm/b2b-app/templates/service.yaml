apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.serviceName }}
  ports:
    - port: 80
      targetPort: 8000
